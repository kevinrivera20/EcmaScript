//Includes se puede utilizar para saber si hay un número incluido dentro de un array.

let numbers = [1,2,3,7,8];

if(numbers.includes(9)) {
    console.log('Sí se encuentra el valor 7');
} else {
    console.log('No se encuentra.');
}

//Característica de ecma7, para sacar la potencia de un número, se usan los dos **.

let base = 4;
let exponent = 3;
let result = base ** exponent;

console.log(result);









